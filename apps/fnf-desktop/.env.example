# Desktop app environment example
# URL to backend API (apps/fnf-api)
FNF_API_URL=http://localhost:3333
# Dev-only: URL to Angular dev server (apps/fnf)
FNF_DEV_SERVER_URL=http://localhost:4200
# Allow DevTools in packaged builds (1|true|yes|on)
FNF_ENABLE_DEVTOOLS=

# Internal HTTP server (renderer-main communication)
# Host defaults to 127.0.0.1; Port defaults to 4765 if unset
FNF_DESKTOP_HTTP_HOST=127.0.0.1
FNF_DESKTOP_HTTP_PORT=4765

# macOS signing/notarization credentials (choose Apple ID or API key)
# Option A – Apple ID (for local builds)
# APPLE_ID example: your-apple-id@example.com
APPLE_ID=
# APPLE_APP_SPECIFIC_PASSWORD example: xxxx-xxxx-xxxx-xxxx
APPLE_APP_SPECIFIC_PASSWORD=
# APPLE_TEAM_ID example: YOURTEAMID
APPLE_TEAM_ID=

# Option B – App Store Connect API key (for CI)
# APPLE_API_KEY example: /absolute/path/to/AuthKey_ABCDEF1234.p8
APPLE_API_KEY=
# APPLE_API_KEY_ID example: ABCDEF1234
APPLE_API_KEY_ID=
# APPLE_API_ISSUER example: 00000000-0000-0000-0000-000000000000
APPLE_API_ISSUER=



# Optional packaging toggles
# When true, skips notarization even if credentials are present (used by pack:unsigned)
SKIP_NOTARIZE=
# When false, prevents electron-builder from auto-discovering a signing identity
CSC_IDENTITY_AUTO_DISCOVERY=
