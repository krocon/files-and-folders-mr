{
  "name": "screenshot",
  "version": "1.0.0",
  "description": "Developer utility for taking screenshots",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node dist/index.js",
    "screenshot": "npm run build && node dist/index.js",
    "dev": "tsc --watch",
    "ic": "npx puppeteer browsers install chrome"
  },
  "dependencies": {
    "puppeteer": "24.16.2",
    "node-fetch": "^3.3.2"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.8.3"
  }
}